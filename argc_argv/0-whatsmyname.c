#include <stdio.h>

/**
 * main - prints the name of the binaray
 * @argc: number of command line agruments passed
 * @argv: array of command line arguments passed
 * Return: 0 on succsess
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s", argv[0]);
	return (0);
}
